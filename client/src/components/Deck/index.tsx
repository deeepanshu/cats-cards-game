import React from 'react';
import { getRandomIntFromRange } from '@lib/utils';

const Deck = () => {
    const pickFromDeck = () => {};

    return (
        <button onClick={pickFromDeck}>
            Click to pick card
        </button>
    );
}

export default Deck;