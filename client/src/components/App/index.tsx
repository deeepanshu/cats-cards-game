import React from 'react';

import Deck from '@components/Deck';
import Header from '@components/Shared/Header';

const App = () => (
    <React.Fragment>
        <Header />
        <Deck />
    </React.Fragment>
);

export default App;